import UsersClient from "@/components/users/UserClient";

export const metadata = {
    title: "Gestor • Usuários",
    description: "Lista de usuários (gestor)",
};

export default function ManagerUsersPage() {
    return <UsersClient />;
}
